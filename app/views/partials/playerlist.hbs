<div class="row">
      <div class="col s6">
            <ul class="collection">
            {{#each players}}
                <li class="collection-item">
                    {{ name }}
                    {{ score }}
                </li>
            {{/each}}
            </ul>
        </div>
</div>